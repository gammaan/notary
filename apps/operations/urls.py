from django.urls import path

from operations.views import (
    AppointmentListPrintView,
    AppointmentListView,
    AuditLogListPrintView,
    AuditLogListView,
    CalendarView,
    ClientCreateView,
    ClientDetailView,
    ClientListPrintView,
    ClientListView,
    ClientUpdateView,
    DashboardView,
    DocumentDownloadView,
    DocumentListView,
    DocumentQuickActionView,
    DocumentUpdateView,
    MatterCompleteView,
    MatterDetailView,
    MatterDocumentsPrintView,
    MatterDocumentsView,
    MatterFinancesView,
    MatterListPrintView,
    MatterListView,
    MatterStatusView,
    MatterUpdateView,
    MatterWizardView,
    StaffLoginView,
    StaffLogoutView,
    StaffProfileView,
    TransactionListPrintView,
    TransactionListView,
    GeneralTransactionCreateView,
    TransactionQuickActionView,
    TransactionReceiptView,
    TransactionUpdateView,
)

app_name = "staff"

urlpatterns = [
    path("login/", StaffLoginView.as_view(), name="login"),
    path("logout/", StaffLogoutView.as_view(), name="logout"),
    path("", DashboardView.as_view(), name="dashboard"),
    path("calendar/", CalendarView.as_view(), name="calendar"),
    path("appointments/", AppointmentListView.as_view(), name="appointment_list"),
    path("appointments/print/", AppointmentListPrintView.as_view(), name="appointment_list_print"),
    path("profile/", StaffProfileView.as_view(), name="profile"),
    path("audit-log/", AuditLogListView.as_view(), name="audit_log"),
    path("audit-log/print/", AuditLogListPrintView.as_view(), name="audit_log_print"),
    path("clients/", ClientListView.as_view(), name="client_list"),
    path("clients/print/", ClientListPrintView.as_view(), name="client_list_print"),
    path("clients/new/", ClientCreateView.as_view(), name="client_create"),
    path("clients/<uuid:pk>/", ClientDetailView.as_view(), name="client_detail"),
    path("clients/<uuid:pk>/edit/", ClientUpdateView.as_view(), name="client_edit"),
    path("matters/", MatterListView.as_view(), name="matter_list"),
    path("matters/print/", MatterListPrintView.as_view(), name="matter_list_print"),
    path("matters/new/", MatterWizardView.as_view(), {"step": "client"}, name="matter_create"),
    path("matters/new/<slug:step>/", MatterWizardView.as_view(), name="matter_wizard"),
    path("matters/<uuid:pk>/", MatterDetailView.as_view(), name="matter_detail"),
    path("matters/<uuid:pk>/status/", MatterStatusView.as_view(), name="matter_status"),
    path("matters/<uuid:pk>/edit/", MatterUpdateView.as_view(), name="matter_edit"),
    path("matters/<uuid:pk>/documents/", MatterDocumentsView.as_view(), name="matter_documents"),
    path("matters/<uuid:pk>/finances/", MatterFinancesView.as_view(), name="matter_finances"),
    path("matters/<uuid:pk>/complete/", MatterCompleteView.as_view(), name="matter_complete"),
    path("documents/<uuid:pk>/action/", DocumentQuickActionView.as_view(), name="document_action"),
    path("documents/<uuid:pk>/download/", DocumentDownloadView.as_view(), name="document_download"),
    path("documents/<uuid:pk>/edit/", DocumentUpdateView.as_view(), name="document_edit"),
    path("transactions/<uuid:pk>/action/", TransactionQuickActionView.as_view(), name="transaction_action"),
    path("transactions/<uuid:pk>/edit/", TransactionUpdateView.as_view(), name="transaction_edit"),
    path("transactions/<uuid:pk>/receipt/", TransactionReceiptView.as_view(), name="transaction_receipt"),
    path("matters/<uuid:pk>/print/documents/", MatterDocumentsPrintView.as_view(), name="matter_print_documents"),
    path("documents/", DocumentListView.as_view(), name="document_list"),
    path("transactions/", TransactionListView.as_view(), name="transaction_list"),
    path("transactions/print/", TransactionListPrintView.as_view(), name="transaction_list_print"),
    path("transactions/new/", GeneralTransactionCreateView.as_view(), name="transaction_create"),
]
