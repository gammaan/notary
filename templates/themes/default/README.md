# Site theme templates

Place site-specific template overrides here. The active public theme CSS is selected via `SITE_THEME` in settings (`theme-1`, `theme-2`, `theme-3`).

Example override path:

```
templates/themes/default/pages/home.html
```

Copy this folder when starting a new client or notary deployment.
